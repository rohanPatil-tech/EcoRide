<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Eco Stats</title>
    <%- include('favicon.ejs') %>
    <link rel="stylesheet" href="/styles.css"> <!-- Link to your external CSS (if needed) -->
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f9;
            color: #333;
        }

        .container {
            width: 80%;
            margin: 50px auto;
            padding: 20px;
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .stats-heading {
            text-align: center;
            font-size: 2.5rem;
            color: #2ecc71;
            margin-bottom: 40px;
        }

        .stats-list {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .stat-card {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            text-align: center;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .stat-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2);
        }

        .stat-title {
            font-size: 1.5rem;
            font-weight: bold;
            color: #2ecc71;
            margin-bottom: 10px;
        }

        .stat-value {
            font-size: 2rem;
            font-weight: 600;
            color: #333;
        }

        .stat-description {
            font-size: 1rem;
            color: #888;
            margin-top: 10px;
        }

        .button-container {
            text-align: center;
        }

        .btn {
            background-color: #2ecc71;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn:hover {
            background-color: #27ae60;
        }
        .highlighted-count {
            color: #2ecc71; /* Green color */
            font-weight: bold;
            font-size: 1.5rem; /* Slightly larger font */
            display: inline-block;
            margin-left: 8px; /* Add spacing between the company name and the count */
            transition: transform 0.3s ease, color 0.3s ease;
        }

        .highlighted-count:hover {
            transform: scale(1.2); /* Grows slightly when hovered */
            color: #27ae60; /* Slightly darker green */
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="stats-heading">Your Eco Stats</h1>

        <div class="stats-list">
            <!-- Total Trips -->
            <div class="stat-card">
                <div class="stat-title">Total Trips</div>
                <div class="stat-value"><%= result.TotalTrips %></div>
                <div class="stat-description">Total number of trips taken</div>
            </div>

            <!-- Average Mileage -->
            <div class="stat-card">
                <div class="stat-title">Average Mileage</div>
                
                <div class="stat-value">
                    <%= result.AverageMileage ? result.AverageMileage + 'MPG' : '0.00 MPG' %>
                  </div>
                <div class="stat-description">Average miles per gallon</div>
            </div>

            <!-- Longest Trip Hours -->
            <div class="stat-card">
                <div class="stat-title">Longest Trip Hours</div>
                <div class="stat-value">
                    <%= result.LongestTripHours ? result.LongestTripHours + ' hrs' : '0 hrs' %>
                  </div>
                <div class="stat-description">Longest duration of a single trip</div>
            </div>

            <!-- Total Cost -->
            <div class="stat-card">
                <div class="stat-title">Total Cost</div>
                <div class="stat-value">
                    $<%= result.TotalCost ? result.TotalCost : '0.00' %>
                  </div>
                <div class="stat-description">Total amount spent on trips</div>
            </div>

            <!-- Favorite Car Company and Bookings -->
            <div class="stat-card">
                <div class="stat-title">Favorite Car Brand</div>
                <div class="stat-value">
                    <%= result.FavoriteCarCompany ? result.FavoriteCarCompany : '--' %>
                  </div>
                <div class="stat-description">Total number of bookings <span class="highlighted-count"><%= result.FavoriteCarCompanyBookings %></span></div>
                
            </div>
        </div>

        <div class="button-container">
            <button class="btn" onclick="window.location.href='/<%=username%>/<%=role%>/profile'">Back to Profile</button>
        </div>
    </div>
</body>
</html>
